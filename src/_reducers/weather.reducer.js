const initialState = {
  error: null,
  isLoaded: false,
  data: {},
};

export default function WeatherReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
